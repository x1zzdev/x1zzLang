
type AirQuality = {
  station: string,
  date: string,
  pm10: Option<float>,
  pm25: Option<float>,
  o3: Option<float>,
};


v result = load("examples/seoul_air_2026.csv") :: AirQuality
  |> filter(pm25 > 15)
  |> select([station, date, pm25])
  |> count;
